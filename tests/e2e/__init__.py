"""End-to-end (E2E) integration tests for agent-sandbox.

These tests exercise the full stack against a *real* container runtime
(Docker or Podman).  They are automatically skipped when no runtime is
available on PATH.
"""
