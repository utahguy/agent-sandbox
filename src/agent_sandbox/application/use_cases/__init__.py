"""Application use cases for agent_sandbox.

Each use case orchestrates domain logic via ports, with no direct knowledge
of infrastructure adapters.

Use cases defined:
  - ParseConfigUseCase: parse raw config text into a typed SandboxConfig
"""
